" Make builtin `syntax/go.vim` highlight function names.
"
" By default these are off (set to 0), so `goFunction`/`goFunctionCall`
" highlight overrides won't be visible.

let g:go_highlight_functions = 1
let g:go_highlight_function_calls = 1

